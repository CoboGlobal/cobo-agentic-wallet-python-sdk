# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_agentic_wallet_api.models.faucet_token_info import FaucetTokenInfo
from typing import Set
from typing_extensions import Self


class FaucetTokenItem(BaseModel):
    """
    FaucetTokenItem
    """  # noqa: E501

    chain_id: StrictStr = Field(
        description="The Cobo chain ID for this group of faucet tokens (for example, `SOL` or `SETH`). Call the List supported chains operation (`GET /metadata/chains`) to retrieve all supported chain IDs."
    )
    tokens: Optional[Dict[str, FaucetTokenInfo]] = Field(
        default=None,
        description='A map of token IDs to their faucet configuration (for example, `{"SETH_USDC": {"deposit_amount": "100", "daily_limit": "500"}}`).',
    )
    __properties: ClassVar[List[str]] = ["chain_id", "tokens"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FaucetTokenItem from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in tokens (dict)
        _field_dict = {}
        if self.tokens:
            for _key in self.tokens:
                if self.tokens[_key]:
                    _field_dict[_key] = self.tokens[_key].to_dict()
            _dict["tokens"] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FaucetTokenItem from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "chain_id": obj.get("chain_id"),
                "tokens": dict(
                    (_k, FaucetTokenInfo.from_dict(_v)) for _k, _v in obj["tokens"].items()
                )
                if obj.get("tokens") is not None
                else None,
            }
        )
        return _obj
