# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class TransferSubmitResult(BaseModel):
    """
    Transfer submission response payload.
    """  # noqa: E501

    id: Optional[StrictStr] = Field(
        default=None,
        description="User transaction UUID. This ID is created at submission time, including `pending_approval` cases.",
    )
    idempotent: Optional[StrictBool] = Field(
        default=False,
        description="Whether this response was returned from a previously submitted request with the same `request_id`. `true`: idempotent replay. `false`: new submission.",
    )
    request_id: Optional[StrictStr] = Field(
        default=None,
        description="Client-supplied request identifier used for idempotency and request correlation. This is not the transaction record ID.",
    )
    pending_operation_id: Optional[StrictStr] = Field(
        default=None,
        description="The pending operation ID when `status=pending_approval`. Use this to track the approval via the List pending operations operation.",
    )
    approval_id: Optional[StrictStr] = Field(
        default=None,
        description="The Approval V2 record ID when `status=pending_approval`. Use this to track the approval via the Approvals operations.",
    )
    transaction_hash: Optional[StrictStr] = Field(
        default=None, description="The on-chain transaction hash, if available."
    )
    status: StrictInt = Field(
        description="The user transaction status code (`UserTransactionStatus`). Typical values: 100 (pending approval), 300 (processing), 400 (pending), 900 (success), 901 (failed), 902 (rejected), 903 (cancelled)."
    )
    status_display: Optional[StrictStr] = Field(
        default="",
        description="The human-readable user transaction status (for example, `Initiated`, `Processing`, `Success`, `Failed`).",
    )
    __properties: ClassVar[List[str]] = [
        "id",
        "idempotent",
        "request_id",
        "pending_operation_id",
        "approval_id",
        "transaction_hash",
        "status",
        "status_display",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TransferSubmitResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TransferSubmitResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "id": obj.get("id"),
                "idempotent": obj.get("idempotent") if obj.get("idempotent") is not None else False,
                "request_id": obj.get("request_id"),
                "pending_operation_id": obj.get("pending_operation_id"),
                "approval_id": obj.get("approval_id"),
                "transaction_hash": obj.get("transaction_hash"),
                "status": obj.get("status"),
                "status_display": obj.get("status_display")
                if obj.get("status_display") is not None
                else "",
            }
        )
        return _obj
