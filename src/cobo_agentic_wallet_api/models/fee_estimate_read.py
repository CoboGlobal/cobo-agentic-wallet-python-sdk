# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.2.4
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class FeeEstimateRead(BaseModel):
    """
    Standardized fee estimate view (supports both EVM and SOL fields).
    """  # noqa: E501

    max_fee_per_gas: Optional[StrictStr] = Field(
        default=None, description="The maximum total fee per gas for EIP-1559 transactions, in wei."
    )
    max_priority_fee_per_gas: Optional[StrictStr] = Field(
        default=None,
        description="The maximum priority fee (tip) per gas for EIP-1559 transactions, in wei.",
    )
    gas_limit: Optional[StrictStr] = Field(
        default=None, description="The estimated gas limit for EIP-1559 transactions."
    )
    compute_unit_price: Optional[StrictStr] = Field(
        default=None,
        description="The price per compute unit for Solana transactions, in microlamports.",
    )
    compute_unit_limit: Optional[StrictStr] = Field(
        default=None, description="The maximum compute units for Solana transactions."
    )
    fee_amount: Optional[StrictStr] = Field(
        default=None, description="The total estimated fee amount as a decimal string."
    )
    __properties: ClassVar[List[str]] = [
        "max_fee_per_gas",
        "max_priority_fee_per_gas",
        "gas_limit",
        "compute_unit_price",
        "compute_unit_limit",
        "fee_amount",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FeeEstimateRead from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FeeEstimateRead from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "max_fee_per_gas": obj.get("max_fee_per_gas"),
                "max_priority_fee_per_gas": obj.get("max_priority_fee_per_gas"),
                "gas_limit": obj.get("gas_limit"),
                "compute_unit_price": obj.get("compute_unit_price"),
                "compute_unit_limit": obj.get("compute_unit_limit"),
                "fee_amount": obj.get("fee_amount"),
            }
        )
        return _obj
