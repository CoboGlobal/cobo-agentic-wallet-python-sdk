# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cobo_agentic_wallet_api.models.fee_request import FeeRequest
from typing import Set
from typing_extensions import Self


class TransferCreate(BaseModel):
    """
    Request payload for token transfer.
    """  # noqa: E501

    dst_addr: Annotated[str, Field(min_length=1, strict=True, max_length=255)] = Field(
        description="The destination on-chain address."
    )
    amount: Annotated[str, Field(min_length=1, strict=True, max_length=100)] = Field(
        description="The transfer amount as a decimal string (for example, `1.5`)."
    )
    token_id: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=100)]] = Field(
        default="SETH",
        description="The Cobo token ID to transfer (for example, `SETH`, `SETH_USDC`, `SOLDEV_SOL_USDC`). Use the List supported assets operation to retrieve all supported token IDs. Defaults to `SETH`.",
    )
    chain_id: Optional[Annotated[str, Field(strict=True, max_length=100)]] = Field(
        default=None, description="The chain ID. Automatically resolved from `token_id` if omitted."
    )
    request_id: Optional[Annotated[str, Field(strict=True, max_length=255)]] = Field(
        default=None,
        description="A client-supplied identifier for idempotency. If a transfer with the same `request_id` already exists for the same principal, the existing record is returned.",
    )
    fee: Optional[FeeRequest] = None
    src_addr: Optional[Annotated[str, Field(strict=True, max_length=255)]] = Field(
        default=None,
        description="The source address to transfer from. If omitted, the server auto-selects the wallet address on this chain with the highest available `token_id` balance (explicit `src_addr` always takes precedence).",
    )
    sponsor: Optional[StrictBool] = Field(
        default=None,
        description="Whether to use Cobo Gasless (Fee Station) to cover gas fees. `true`: gas fees are sponsored. `false`: gas fees are paid from the wallet. Defaults to `true` for human-owned wallets, `false` for agent-owned wallets.",
    )
    gas_provider: Optional[Annotated[str, Field(strict=True, max_length=50)]] = Field(
        default=None,
        description="The gas provider to use for sponsored transactions. If omitted, the server default is used.",
    )
    description: Optional[Annotated[str, Field(strict=True, max_length=512)]] = Field(
        default=None,
        description="Optional note stored locally and sent to WaaS as the transaction `description` (transfer).",
    )
    __properties: ClassVar[List[str]] = [
        "dst_addr",
        "amount",
        "token_id",
        "chain_id",
        "request_id",
        "fee",
        "src_addr",
        "sponsor",
        "gas_provider",
        "description",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TransferCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of fee
        if self.fee:
            _dict["fee"] = self.fee.to_dict()
        # set to None if fee (nullable) is None
        # and model_fields_set contains the field
        if self.fee is None and "fee" in self.model_fields_set:
            _dict["fee"] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TransferCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "dst_addr": obj.get("dst_addr"),
                "amount": obj.get("amount"),
                "token_id": obj.get("token_id") if obj.get("token_id") is not None else "SETH",
                "chain_id": obj.get("chain_id"),
                "request_id": obj.get("request_id"),
                "fee": FeeRequest.from_dict(obj["fee"]) if obj.get("fee") is not None else None,
                "src_addr": obj.get("src_addr"),
                "sponsor": obj.get("sponsor"),
                "gas_provider": obj.get("gas_provider"),
                "description": obj.get("description"),
            }
        )
        return _obj
