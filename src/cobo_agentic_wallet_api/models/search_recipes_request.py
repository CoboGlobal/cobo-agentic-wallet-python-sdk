# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_agentic_wallet_api.models.knowledge_search_type import KnowledgeSearchType
from cobo_agentic_wallet_api.models.recipe_search_source import RecipeSearchSource
from typing import Set
from typing_extensions import Self


class SearchRecipesRequest(BaseModel):
    """
    Request payload for recipe knowledge-base search.
    """  # noqa: E501

    query: StrictStr
    source: Optional[RecipeSearchSource] = Field(
        default=None, description="Search source (only 'knowledge' is supported)"
    )
    limit: Optional[StrictInt] = Field(default=1, description="Max results for knowledge search")
    chain: Optional[StrictStr] = Field(
        default=None,
        description="Filter recipes by chain symbol (e.g. 'ethereum', 'solana', 'bitcoin')",
    )
    token: Optional[StrictStr] = Field(
        default=None, description="Filter recipes by token symbol (e.g. 'ETH', 'USDT', 'BTC')"
    )
    keywords: Optional[List[StrictStr]] = Field(
        default=None, description="List of keywords for filtering documents during keyword search"
    )
    search_type: Optional[KnowledgeSearchType] = Field(
        default=None, description="Search type: 'vector', 'keyword', or 'hybrid'"
    )
    __properties: ClassVar[List[str]] = [
        "query",
        "source",
        "limit",
        "chain",
        "token",
        "keywords",
        "search_type",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SearchRecipesRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SearchRecipesRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "query": obj.get("query"),
                "source": obj.get("source"),
                "limit": obj.get("limit") if obj.get("limit") is not None else 1,
                "chain": obj.get("chain"),
                "token": obj.get("token"),
                "keywords": obj.get("keywords"),
                "search_type": obj.get("search_type"),
            }
        )
        return _obj
