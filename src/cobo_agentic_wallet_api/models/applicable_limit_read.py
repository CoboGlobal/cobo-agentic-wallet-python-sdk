# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self


class ApplicableLimitRead(BaseModel):
    """
    Represents a spending limit that applies to a given operation, returned in dry-run and pending operation responses.
    """  # noqa: E501

    subject_type: StrictStr = Field(
        description="The subject type for this limit (for example, `principal`, `delegation`)."
    )
    subject_id: StrictStr = Field(description="The subject ID this limit applies to.")
    operation_type: StrictStr = Field(
        description="The operation type this limit governs (for example, `transfer`, `contract_call`)."
    )
    window: StrictStr = Field(
        description="The rolling time window for this limit (for example, `rolling_1h`, `rolling_24h`)."
    )
    metric: StrictStr = Field(
        description="The metric being limited (for example, `amount`, `tx_count`)."
    )
    limit_value: StrictStr = Field(description="The limit threshold as a decimal string.")
    increment: StrictStr = Field(
        description="The amount this operation would contribute toward the limit, as a decimal string."
    )
    source_policy_id: StrictStr = Field(description="The ID of the policy that defined this limit.")
    __properties: ClassVar[List[str]] = [
        "subject_type",
        "subject_id",
        "operation_type",
        "window",
        "metric",
        "limit_value",
        "increment",
        "source_policy_id",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ApplicableLimitRead from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ApplicableLimitRead from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "subject_type": obj.get("subject_type"),
                "subject_id": obj.get("subject_id"),
                "operation_type": obj.get("operation_type"),
                "window": obj.get("window"),
                "metric": obj.get("metric"),
                "limit_value": obj.get("limit_value"),
                "increment": obj.get("increment"),
                "source_policy_id": obj.get("source_policy_id"),
            }
        )
        return _obj
