# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_agentic_wallet_api.models.mpp_session_info import MppSessionInfo
from cobo_agentic_wallet_api.models.payment_protocol import PaymentProtocol
from typing import Set
from typing_extensions import Self


class PaymentResult(BaseModel):
    """
    Unified payment result.  CLI uses retry_headers to retry the original request.
    """  # noqa: E501

    id: Optional[StrictStr] = Field(
        default=None, description="Unique identifier assigned to this payment record."
    )
    idempotent: Optional[StrictBool] = Field(
        default=False,
        description="True if this result was served from a cached response for a previously processed request_id; false if it was freshly processed.",
    )
    request_id: Optional[StrictStr] = Field(
        default=None, description="Idempotency key echoed back from the original payment request."
    )
    protocol: PaymentProtocol = Field(description="'x402' or 'mpp'")
    status: StrictStr = Field(description="completed / submitted / failed")
    retry_headers: Optional[Dict[str, StrictStr]] = Field(
        default=None,
        description="Headers to set when retrying the original request. x402: {'PAYMENT-SIGNATURE': '...'}. MPP: {'Authorization': 'Payment <cred>'}.",
    )
    mpp_session_info: Optional[MppSessionInfo] = Field(
        default=None,
        description="MPP session details populated only when the action is 'open'. Null for all other actions.",
    )
    tx_hash: Optional[StrictStr] = Field(
        default=None,
        description="On-chain transaction hash returned for request_close and withdraw actions. Null for all other actions.",
    )
    __properties: ClassVar[List[str]] = [
        "id",
        "idempotent",
        "request_id",
        "protocol",
        "status",
        "retry_headers",
        "mpp_session_info",
        "tx_hash",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of mpp_session_info
        if self.mpp_session_info:
            _dict["mpp_session_info"] = self.mpp_session_info.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "id": obj.get("id"),
                "idempotent": obj.get("idempotent") if obj.get("idempotent") is not None else False,
                "request_id": obj.get("request_id"),
                "protocol": obj.get("protocol"),
                "status": obj.get("status"),
                "retry_headers": obj.get("retry_headers"),
                "mpp_session_info": MppSessionInfo.from_dict(obj["mpp_session_info"])
                if obj.get("mpp_session_info") is not None
                else None,
                "tx_hash": obj.get("tx_hash"),
            }
        )
        return _obj
