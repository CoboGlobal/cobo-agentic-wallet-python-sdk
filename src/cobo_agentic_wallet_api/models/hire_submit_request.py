# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cobo_agentic_wallet_api.models.hire_pact_spec_input import HirePactSpecInput
from cobo_agentic_wallet_api.models.hire_submission_record import HireSubmissionRecord
from cobo_agentic_wallet_api.models.negotiated_spec_patch_entry_input import (
    NegotiatedSpecPatchEntryInput,
)
from typing import Set
from typing_extensions import Self


class HireSubmitRequest(BaseModel):
    """
    Create a new hire pact submission.
    """  # noqa: E501

    spec: HirePactSpecInput
    service_principal_id: StrictStr
    pact_agent_principal_id: Optional[StrictStr] = None
    protocol_pact_id: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=255)]] = (
        None
    )
    submission: HireSubmissionRecord
    negotiated_patch_history: Optional[List[NegotiatedSpecPatchEntryInput]] = None
    __properties: ClassVar[List[str]] = [
        "spec",
        "service_principal_id",
        "pact_agent_principal_id",
        "protocol_pact_id",
        "submission",
        "negotiated_patch_history",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of HireSubmitRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of spec
        if self.spec:
            _dict["spec"] = self.spec.to_dict()
        # override the default output from pydantic by calling `to_dict()` of submission
        if self.submission:
            _dict["submission"] = self.submission.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in negotiated_patch_history (list)
        _items = []
        if self.negotiated_patch_history:
            for _item in self.negotiated_patch_history:
                if _item:
                    _items.append(_item.to_dict())
            _dict["negotiated_patch_history"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of HireSubmitRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "spec": HirePactSpecInput.from_dict(obj["spec"])
                if obj.get("spec") is not None
                else None,
                "service_principal_id": obj.get("service_principal_id"),
                "pact_agent_principal_id": obj.get("pact_agent_principal_id"),
                "protocol_pact_id": obj.get("protocol_pact_id"),
                "submission": HireSubmissionRecord.from_dict(obj["submission"])
                if obj.get("submission") is not None
                else None,
                "negotiated_patch_history": [
                    NegotiatedSpecPatchEntryInput.from_dict(_item)
                    for _item in obj["negotiated_patch_history"]
                ]
                if obj.get("negotiated_patch_history") is not None
                else None,
            }
        )
        return _obj
