# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, ValidationError, field_validator
from typing import Any, Optional
from cobo_agentic_wallet_api.models.eip1559_fee_request import EIP1559FeeRequest
from cobo_agentic_wallet_api.models.sol_fee_request import SOLFeeRequest
from typing import Union, Set, Dict
from typing_extensions import Self

FEE_ONE_OF_SCHEMAS = ["EIP1559FeeRequest", "SOLFeeRequest"]


class Fee(BaseModel):
    """
    Optional fee parameters (EIP-1559 or SOL). If omitted, the network default is used.
    """

    # data type: EIP1559FeeRequest
    oneof_schema_1_validator: Optional[EIP1559FeeRequest] = None
    # data type: SOLFeeRequest
    oneof_schema_2_validator: Optional[SOLFeeRequest] = None
    actual_instance: Optional[Union[EIP1559FeeRequest, SOLFeeRequest]] = None
    one_of_schemas: Set[str] = {"EIP1559FeeRequest", "SOLFeeRequest"}

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        instance = Fee.model_construct()
        error_messages = []
        match = 0
        # validate data type: EIP1559FeeRequest
        if not isinstance(v, EIP1559FeeRequest):
            error_messages.append(f"Error! Input type `{type(v)}` is not `EIP1559FeeRequest`")
        else:
            match += 1
        # validate data type: SOLFeeRequest
        if not isinstance(v, SOLFeeRequest):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SOLFeeRequest`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in Fee with oneOf schemas: EIP1559FeeRequest, SOLFeeRequest. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in Fee with oneOf schemas: EIP1559FeeRequest, SOLFeeRequest. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        return instance
        # deserialize data into EIP1559FeeRequest
        try:
            instance.actual_instance = EIP1559FeeRequest.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SOLFeeRequest
        try:
            instance.actual_instance = SOLFeeRequest.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into Fee with oneOf schemas: EIP1559FeeRequest, SOLFeeRequest. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            return instance
            # raise ValueError("No match found when deserializing the JSON string into Fee with oneOf schemas: EIP1559FeeRequest, SOLFeeRequest. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], EIP1559FeeRequest, SOLFeeRequest]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
