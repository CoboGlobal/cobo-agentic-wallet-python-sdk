# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Set
from typing_extensions import Self


class WebhookDLQRead(BaseModel):
    """
    Dead-letter queue row response.
    """  # noqa: E501

    id: StrictStr = Field(description="Unique identifier of this dead-letter queue entry.")
    outbox_entry_id: StrictStr = Field(
        description="UUID of the originating outbox entry that failed delivery."
    )
    event_id: StrictStr = Field(
        description="UUID of the webhook event that could not be delivered."
    )
    event_type: StrictStr = Field(
        description="String identifier of the event type, e.g. 'transaction.completed'."
    )
    payload: Dict[str, Any] = Field(
        description="Full event payload that was attempted for delivery."
    )
    registration_id: Optional[StrictStr] = Field(
        default=None,
        description="UUID of the webhook registration targeted for delivery, if available.",
    )
    last_error: StrictStr = Field(
        description="Human-readable description of the last delivery error encountered."
    )
    failed_at: datetime = Field(
        description="Timestamp when the most recent delivery attempt failed."
    )
    __properties: ClassVar[List[str]] = [
        "id",
        "outbox_entry_id",
        "event_id",
        "event_type",
        "payload",
        "registration_id",
        "last_error",
        "failed_at",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WebhookDLQRead from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WebhookDLQRead from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "id": obj.get("id"),
                "outbox_entry_id": obj.get("outbox_entry_id"),
                "event_id": obj.get("event_id"),
                "event_type": obj.get("event_type"),
                "payload": obj.get("payload"),
                "registration_id": obj.get("registration_id"),
                "last_error": obj.get("last_error"),
                "failed_at": obj.get("failed_at"),
            }
        )
        return _obj
