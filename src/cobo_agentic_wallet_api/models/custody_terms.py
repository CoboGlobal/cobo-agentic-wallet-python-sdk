# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from cobo_agentic_wallet_api.models.hire_participant_role import HireParticipantRole
from typing import Optional, Set
from typing_extensions import Self


class CustodyTerms(BaseModel):
    """
    Reviewed custody rules for the pact vault.
    """  # noqa: E501

    model: Annotated[str, Field(strict=True)]
    threshold: Annotated[int, Field(le=2, strict=True, ge=2)]
    participants: Annotated[List[HireParticipantRole], Field(min_length=3, max_length=3)]
    vault_address: Annotated[str, Field(min_length=1, strict=True, max_length=255)]
    normal_settlement_signers: Annotated[
        List[HireParticipantRole], Field(min_length=2, max_length=2)
    ]
    refund_or_dispute_signers: Annotated[
        List[HireParticipantRole], Field(min_length=2, max_length=2)
    ]
    __properties: ClassVar[List[str]] = [
        "model",
        "threshold",
        "participants",
        "vault_address",
        "normal_settlement_signers",
        "refund_or_dispute_signers",
    ]

    @field_validator("model")
    def model_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^mpc_2_of_3$", value):
            raise ValueError(r"must validate the regular expression /^mpc_2_of_3$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CustodyTerms from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CustodyTerms from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "model": obj.get("model"),
                "threshold": obj.get("threshold"),
                "participants": obj.get("participants"),
                "vault_address": obj.get("vault_address"),
                "normal_settlement_signers": obj.get("normal_settlement_signers"),
                "refund_or_dispute_signers": obj.get("refund_or_dispute_signers"),
            }
        )
        return _obj
