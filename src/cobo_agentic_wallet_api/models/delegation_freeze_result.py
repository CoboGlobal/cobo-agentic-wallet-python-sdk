# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 0.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_agentic_wallet_api.models.freeze_scope import FreezeScope
from typing import Set
from typing_extensions import Self


class DelegationFreezeResult(BaseModel):
    """
    Response payload returned after a freeze operation.
    """  # noqa: E501

    affected_count: StrictInt = Field(description="The number of delegations that were frozen.")
    scope: FreezeScope = Field(description="The scope used for this freeze operation.")
    ids: Optional[List[StrictStr]] = Field(
        default=None, description="The IDs of the delegations that were frozen."
    )
    audit_event_id: StrictStr = Field(description="The audit log event ID for this operation.")
    frozen_at: datetime = Field(
        description="The timestamp when the delegations were frozen, in ISO 8601 format."
    )
    __properties: ClassVar[List[str]] = [
        "affected_count",
        "scope",
        "ids",
        "audit_event_id",
        "frozen_at",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of DelegationFreezeResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of DelegationFreezeResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "affected_count": obj.get("affected_count"),
                "scope": obj.get("scope"),
                "ids": obj.get("ids"),
                "audit_event_id": obj.get("audit_event_id"),
                "frozen_at": obj.get("frozen_at"),
            }
        )
        return _obj
