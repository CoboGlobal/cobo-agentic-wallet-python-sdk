# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.2.4
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cobo_agentic_wallet_api.models.vault_group_type import VaultGroupType
from cobo_agentic_wallet_api.models.wallet_type import WalletType
from typing import Set
from typing_extensions import Self


class WalletCreate(BaseModel):
    """
    Request payload for creating a wallet.  Ownership can be selected via `for_owner`: - for_owner=False: create for current principal (Agent)     - for_owner=True: create for current principal's owner (Human),         or fallback to current principal when no owner is linked
    """  # noqa: E501

    wallet_type: WalletType = Field(description="The wallet type. Use `MPC`.")
    name: Annotated[str, Field(min_length=1, strict=True, max_length=255)] = Field(
        description="The wallet name. Max 255 characters."
    )
    group_type: Optional[VaultGroupType] = Field(
        default=None,
        description="The MPC key share scheme. `AGENT` (2-of-2): agent TSS node + Cobo, suitable for automated wallets. `HUMAN` (2-of-2): mobile TSS node + Cobo, suitable for human-controlled wallets. Defaults to `AGENT`.",
    )
    main_node_id: Optional[StrictStr] = Field(
        default=None,
        description="The TSS node ID of the agent's main device. Required when `wallet_type=MPC`.",
    )
    metadata: Optional[Dict[str, Any]] = Field(
        default=None, description="Arbitrary key-value metadata to associate with the wallet."
    )
    for_owner: Optional[StrictBool] = Field(
        default=False,
        description="Whether to create the wallet for the principal's linked owner (`true`) or for the principal itself (`false`). Defaults to `false`.",
    )
    __properties: ClassVar[List[str]] = [
        "wallet_type",
        "name",
        "group_type",
        "main_node_id",
        "metadata",
        "for_owner",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of WalletCreate from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of WalletCreate from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "wallet_type": obj.get("wallet_type"),
                "name": obj.get("name"),
                "group_type": obj.get("group_type"),
                "main_node_id": obj.get("main_node_id"),
                "metadata": obj.get("metadata"),
                "for_owner": obj.get("for_owner") if obj.get("for_owner") is not None else False,
            }
        )
        return _obj
