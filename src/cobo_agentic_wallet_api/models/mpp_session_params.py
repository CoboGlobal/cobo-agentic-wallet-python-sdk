# coding: utf-8

"""
Cobo Agentic Wallet Service

The version of the OpenAPI document: 1.1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_agentic_wallet_api.models.mpp_challenge import MppChallenge
from cobo_agentic_wallet_api.models.mpp_session_action import MppSessionAction
from typing import Set
from typing_extensions import Self


class MppSessionParams(BaseModel):
    """
    Parameters for MPP session actions (open / voucher / close / request_close / withdraw).
    """  # noqa: E501

    action: MppSessionAction = Field(
        description="Session action: 'open' | 'voucher' | 'close' | 'request_close' | 'withdraw'"
    )
    salt: Optional[StrictStr] = Field(
        default=None, description="Random salt for session open (hex string)"
    )
    deposit: Optional[StrictStr] = Field(
        default=None, description="Deposit amount for session open (raw token units)"
    )
    channel_id: Optional[StrictStr] = Field(
        default=None,
        description="Payment channel identifier, required for voucher, close, request_close, and withdraw actions.",
    )
    cumulative_amount: Optional[StrictStr] = Field(
        default=None,
        description="Cumulative total amount paid so far in this channel, expressed in raw token units. Required for voucher actions.",
    )
    payer_address: Optional[StrictStr] = Field(
        default=None,
        description="On-chain address of the payer who opened the payment channel. Required for voucher/close actions.",
    )
    challenge_cache: Optional[MppChallenge] = Field(
        default=None, description="Cached challenge for voucher/close (no 402 re-negotiation)"
    )
    __properties: ClassVar[List[str]] = [
        "action",
        "salt",
        "deposit",
        "channel_id",
        "cumulative_amount",
        "payer_address",
        "challenge_cache",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MppSessionParams from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of challenge_cache
        if self.challenge_cache:
            _dict["challenge_cache"] = self.challenge_cache.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MppSessionParams from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "action": obj.get("action"),
                "salt": obj.get("salt"),
                "deposit": obj.get("deposit"),
                "channel_id": obj.get("channel_id"),
                "cumulative_amount": obj.get("cumulative_amount"),
                "payer_address": obj.get("payer_address"),
                "challenge_cache": MppChallenge.from_dict(obj["challenge_cache"])
                if obj.get("challenge_cache") is not None
                else None,
            }
        )
        return _obj
